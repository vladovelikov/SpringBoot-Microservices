server:
  port: 9296

spring:
  application:
    name: CONFIG-SERVER
  cloud:
    config:
      server:
        git:
          uri: http://github.com/shabbirdwd53/config-server
          clone-on-start: true